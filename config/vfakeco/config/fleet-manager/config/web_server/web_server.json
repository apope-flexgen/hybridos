{
    "systemInformationRequests": [
        {
            "name": "PROCESSES",
            "command": "rpm -qa fims node_fims hybridos_controller modbus_interface mcp storage metrics events dbi web_server influxdb"
        },
        {
            "name": "DISKSPACE",
            "command": "df -h"
        },
        {
            "name": "CPU USAGE",
            "command": "ps -eo pcpu,pid,user,args | sort -k 1 -r | head -10"
        }
    ],
    "todo": "might need to change this back"
}
