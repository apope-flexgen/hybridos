/* eslint-disable */
// TODO: fix lint
// - default is restricted from being used, but this is the correct way
// to re-export a default. can we config this rule?
export { default } from './Websocket/socket.service';
