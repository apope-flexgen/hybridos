// This is a dummy WORKER_PATH (see webpack.config.js) module.
// A worker is not actually required to listen to fims using the
// zeromq messaging library. I/O is already done on a background thread.
export {};
