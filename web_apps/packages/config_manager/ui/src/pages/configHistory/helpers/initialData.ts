/* This is the template we want to use to format the data on the useGetComponentData custom-hook */
const initialData: object = {
    path: {
        customer: 'customer1',
        site: 'any',
        server: 'ess_controller_1',
    },
    configData: {},
}

export default initialData
