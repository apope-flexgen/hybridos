import { PageProps } from '../pageTypes'

const ConfigDiff = ({ pageName }: PageProps) => {
    return (
        <div>
            <div>{pageName}</div>
        </div>
    )
}

export default ConfigDiff
