{
    "inputs": {
        "input": { "uri": "/example/input", "type": "bool" }
    },
    "outputs": {
        "output": { "uri": "/some/output", "flags":["clothed", "post", "sparse", "no_heartbeat"] }
	},
    "metrics": [
        {
            "id": "output",
            "type": "bool", 
            "outputs": "output",
            "expression": "input"
        }
    ]
}